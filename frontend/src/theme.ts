import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#6a11cb',
        },
        secondary: {
            main: '#2575fc',
        },
    },
});

export default theme; 